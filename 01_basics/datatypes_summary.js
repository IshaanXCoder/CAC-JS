// SYMBOL GIVES UNIQUENESS

let id = Symbol('32')
let anotherid = Symbol('32')
console.log(id==anotherid);

// An array is a special variable, which can hold more than one value:

// declaring an array is simple
array_name = ["car", "dog", "cat"]
console.log(array_name);

// obejcts
let myObj = {
    name : "ishaan",
    age : 18,
    city : "gurgaon"
}
console.log(myObj);